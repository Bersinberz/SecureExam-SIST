"1)Shopping Cart Total: A shopping cart contains item prices as [12.99, 5.49, 20.00]. a) Use a built-in function to calculate the total price. b) What is the data type of the result? 2)Take a number as input and use a loop to calculate the factorial by multiplying each number from 1 to the given number
"
"1) User Age Input: A user enters their age using input(). a)Convert the input to an integer. b)Print the age and its data type. 2)Write a Program to accept a number from 1 to 7 and display the name of the day like 1 for Sunday, 2 for Monday and so on.

"
"1) Temperature Rounding: A temperature sensor records 36.6865°C. a) Round it to 2 decimal places using a built-in function. b) What is the data type of the rounded value? 2)Take a number as input and check if it can be divisible by any number from 2 to the square root of the number. If a divisor is found, it is not a prime number 

"
"1) Team Scores: You are given team scores: [120, 130, 115, 150]. a) Use a built-in function to find the highest score. b) What will be the data type of the result? 2) Use a loop to generate the Fibonacci series by adding the previous two numbers. This loop will continue until the generated number exceeds the given number. 

"
"1) Data Type Checker: You receive mixed data from a form: [101, ""John"", 45.6, False]. a) Write a loop that prints each item and its data type. 2) Write a program to check whether a person is eligible for voting or not.(Voting age>=18).

"
"1) ID Conversion: You receive a string ""1023"" from a form as user ID. a) Convert it to an integer and print the type. 2) You would use the conversion formula: Fahrenheit = Celsius * 9/5 + 32, to calculate the equivalent temperature in Fahrenheit.  
"
"1) Employee Salary Update: You have this salary list: [3500.75, 4200.50, 3900.20]. a) Use a built-in function to find the average salary (hint: use sum() and len()). b) What is the type of the final result? 2) You would use moderator in Python, denoted as ‘%’ You will then take the number as input and divide it by 2. If the remainder is 0, the number is even.
"
"1) Type Before and After Conversion: A user inputs their phone number ""9876543210"" as a string. a) Print its type. b)Convert it to an integer, then print the new type. 2) To find the sum of all numbers in a list, You would iterate through each element in the list using a for loop. The total sum can be generated as an output after running total of the numbers encountered. 

"
"1) String Join Operation: You have a list of names: [""Alice"", ""Bob"", ""Charlie""]. a) Join them into a single string with commas using a built-in function. b) What is the data type of the result? 2)Taking a string as input and using string manipulation techniques, such as iteration or slicing, will reverse the order of its characters. Finally, the reversed string can be displayed. 

"
"1) ATM Withdrawal Limit Check: A user tries to withdraw money from an ATM. If the amount is more than ₹10,000, display ""Limit Exceeded"".Write a program using a conditional statement to implement this logic. 2)Compare the original string with its reversed version through string manipulation techniques. If both the strings are identical, the input string is a palindrome. 

"
"1) Even or Odd Counter: A list of numbers is given: [10, 25, 36, 47, 50]. Use a loop and conditional logic to count how many are even and how many are odd. 2) Accept three sides of triangle and check wheatear the triangle is possible or not.

"
"1) Password Checker: A user enters a password. a) If the password is correct, show ""Login Successful"". b) Else, allow up to 3 attempts using a loop. c) After 3 failed attempts, display ""Account Locked"". 2) Write a program which will find all such numbers which are divisible by 7 but are not a multiple of 5 between 2000 and 3200 (both included). The numbers obtained should be printed in a comma-separated sequence on a single line.

"
"1) Number Guessing Game: A secret number is set to 7. Ask the user to guess the number until they get it right. Use a while loop. Display ""Correct!"" when the guess is right. 2)Vowels are very essential characters to form any meaningful word in English dictionary. There are 5 vowels in English language - a, e, i, o u. You are given a random string containing only lowercase letters and you need to find if the string contains ALL the vowels.
"
"1) Print Multiples of 3 between 1 and 50: Use a for loop to print all numbers between 1 and 50 that are multiples of 3.  2) Consider All Lowercase Alphabets of the English language. Here we consider each alphabet from a to z to have a certain weight. The weight of the alphabet a is considered to be 1, b to be 2, c to be 3 and so on until z has a weight of 26. In short, the weight of the alphabet a is 1, and the weight of all other alphabets is the weight of its previous alphabet + 1. Now, you have been given a String S consisting of lowercase English characters. You need to find the summation of weight of each character in this String.

"
"1) Skip Vowels in a Word: Given a word, print each letter using a loop. Skip the vowels (a, e, i, o, u) using continue. 2) Write a Python program that prompts the user to input an integer and raises a Value Error exception if the input is not a valid integer.

"
"1) Find First Divisible Number: Loop through numbers 1 to 100 and print the first number divisible by both 7 and 11. Exit the loop once found. 2) Write a  Python function student_data () that will print the ID of astudent (student_id). If the user passes an argument student_name or student_class the function will print the student name and class.

 "
"1) Student Marks Entry: You are given a list of student names and their marks. Write a program to save them into a CSV file named marks.csv. Each row should have Name and Marks. 2) Write a NumPy program to create an array of integers from 30 to 70.

"
"1) Employee Data Reader: A CSV file employees.csv contains columns: ID, Name, Department. Write a program to read and display each employee’s details line by line. 2)Write a NumPy program to create an array of 10 zeros, 10 ones, and 10 fives. 
"
"1) Attendance Tracker: Create a CSV file attendance.csv that logs student name and attendance status (Present/Absent). Append attendance records using the csv module. 2) Write a Python program to handle a ZeroDivisionError exception when dividing a number by zero.

"
"1) Filter High Scores: From a file scores.csv (columns: Player, Score), read and display only players who scored more than 50. 2) Write a  Python function student_data () that will print the ID of a student (student_id). If the user passes an argument student_name or student_class the function will print the student name and class.
"
"1) Sales Report Generator: A file sales.csv contains Date, Product, Amount. Read the file and calculate the total sales amount. Print the result. 2) Write a Python class to find the three elements that sum to zero from a set of n real numbers. Input array : [-25, -10, -7, -3, 2, 4, 8, 10] Output : [[-10, 2, 8], [-7, -3, 10]]

"
"1) Display CSV as a Table: Read the file products.csv and print the contents in a tabular format using a loop. 2)  Consider All Lowercase Alphabets of the English language. Here we consider each alphabet from a to z to have a certain weight. The weight of the alphabet a is considered to be 1, b to be 2, c to be 3 and so on until z has a weight of 26. In short, the weight of the alphabet a is 1, and the weight of all other alphabets is the weight of its previous alphabet + 1. Now, you have been given a String S consisting of lowercase English characters. You need to find the summation of weight of each character in this String.
"
"1) Update Stock Quantity: You have a stock.csv with Item, Quantity. Read it and create a new file updated_stock.csv where the quantity of each item is increased by 10. 2) Given an array of positive and negative integers, denoting different types of parentheses. The positive numbers denote opening parentheses of type and negative numbers denote closing parentheses of type. Open parentheses must be closed by the same type of parentheses. Open parentheses must be closed in the correct order, i.e., never close an open pair before its inner pair is closed (if it has an inner pair). Thus, [1,2,−2,−1] is balanced, while [1,2,−1,−2] is not balanced.

"
"1) Search by Column Value: In a file library.csv with columns BookID, Title, Author, Write a program to search and display all books by a given author name. 2)Write a Python program to create a person class. Include attributes  like name, country and date of birth. Implement a method to  determine the person’s age. 

"
"1) Write Multiple Rows at Once: A list of dictionaries with employee data is given. Write the entire data to a CSV file using csv.DictWriter. 2) Write a Python class that has two methods: get_String and print_String ,get_String accept a string from the user and print_String prints the string in upper case.
"
"1) Odd-Even and Prime Checker Problem: A school is conducting a math quiz. They want a tool to classify numbers. Task: Accept an integer from the user and determine if it is odd/even and whether it is prime. 2) Create a Bus class that inherits from the Vehicle class. Give the capacity argument of Bus.seating_capacity() a default value of 50.

"
"1) Student Grade Calculator Problem: A university needs to automate grade generation based on exam marks. Task: Get student marks and assign grades (A, B, C, etc.) using conditional statements. Repeat for multiple students. 2) Write a  Python class BankAccount with attributes like account_number, balance, date_of_opening and customer_name, and methods like deposit, withdraw, and check_balance.

"
"1) Retail Billing System Problem: A retail store needs a billing system with discount logic. Task: Input item names, prices, and quantities. Compute total bill and apply a discount if the bill exceeds ₹1000. 2)  Write a Python class Inventory with attributes like item_id, item_name, stock_count, and price, and methods like add_item, update_item, and check_item_details.

"
"1)  Traffic Signal Timer Simulation Problem: A smart signal system requires a simulation for time-based light changes. Task: Write a Python program using a while loop that simulates Red, Yellow, and Green signals in intervals. 2)Write a function in python to read the content from a text file ""poem.txt"" line by line and display the same on screen.

"
"1)  COVID-19 Case Visualizer Problem: Health department shares daily COVID-19 case data in CSV format. Task: Read the CSV, analyze daily case counts, and visualize using pandas and matplotlib. 2)  Write a NumPy program to create an array of 10 zeros, 10 ones, and 10 fives.
"
"1) File-Based Number Analyzer Problem: A text file contains a list of integers. Task: Read the file and count how many numbers are even and how many are odd. 2) Write a Python function that reads a file and prints its content. Implement exception handling for: A.If the file is not found, it should print ""File not found."" B.If there is an input/output error, it should print ""I/O error occurred."""
"1) Traffic Signal Timer Simulation Problem: A smart signal system requires a simulation for time-based light changes. Task: Write a Python program using a while loop that simulates Red, Yellow, and Green signals in intervals. 2) Write a Python function that reads a file and prints its content. Implement exception handling for: If the file contains improperly formatted data   (e.g., trying to convert   text to integer), it should print ""Data format error.""
"
"1) Attendance Tracker Problem: A teacher wants to keep track of present and absent students. Task: Input names and attendance (P/A). Store in a dictionary and summarize attendance using basic statistics. 2) Write a Python code : You need to handle cases where the user inputs a non-numeric value for age. If the input is invalid, raise a custom exception called InvalidAgeError.

"
"1) Marks Comparison System Problem: A teacher wants to compare marks of two students in three subjects. Task: Take input marks, calculate total, average, and determine who scored higher. 2) Write a python code: You are developing an online payment system. The system must handle different exceptions based on specific errors like InsufficientFunds, InvalidCardDetails, and TransactionFailed. These errors should be caught individually to provide meaningful error messages.
"
"1) Product Discount Analyzer Problem: A store offers discounts based on purchase amount. Task: Accept item prices and apply tiered discounts using nested if-else conditions. 2) Write a python code:If there is an error related to insufficient funds, the program should notify the user of the problem. Similarly, if there is an invalid card number, an error should be raised, or if the transaction fails, a different message should be shown.

"
"1) E-Commerce Product Management Problem: An online store wants to manage products and electronics separately. Task: Create a Product class with private price. Add getter/setter. Create subclass Electronics with warranty. 2) Your Python program receives user input for age and wants to check whether the input is valid. The input could be non-numeric, and you should handle such cases by raising a custom exception.

"
"1) Library Book System Problem: A digital library stores books and ebooks. Task: Create class Book with title, author, ISBN. Use encapsulation for price. Subclass EBook adds file_size. 2) In your Python code, you are implementing a function that divides two numbers. The program should not crash when the denominator is zero. Instead, it should catch the division-by-zero error and print a meaningful message to the user.

"
1) School Staff Management Problem: A school keeps records of teachers and students under a unified system. Task: Implement class Person. Create subclasses Teacher and Student. Use method overriding for role display. 2) You are building a python program that checks if a string is a palindrome (a word that reads the same backward as forward).
"1) Smartphone Classification Tool Problem: A tech store wants to classify mobile phones. Task: Create Phone class. Subclass Smartphone adds OS and camera. Use constructors for initialization. 2) You are building a system to check password strength for users registering on a website using Regular Expression.Write a python code for this scenerio.
"
1) Car Rental Application Problem: A system tracks car rentals and rates. Task: Create Car class with constructor and rental method. Use data hiding for internal pricing logic. 2) You are building a system to validate phone numbers. The phone numbers should match a specific format such as: (xxx) xxx-xxxx or xxx-xxx-xxxx where x is a digit.Write a python code for this scenerio
"1) Student Record Keeper Problem: A school stores records in students.txt. Task: Read and write student details. Handle file-not-found or permission errors. 2) You are creating a registration system that requires users to enter a password. The password must: a) Be at least 8 characters long. b) Contain at least one uppercase letter, one lowercase letter, one number, and one special character (e.g., !@#$%^&*).
"
"1) Fibonacci Generator Problem: A math club wants to generate Fibonacci series efficiently. Task: Create a custom iterator that prints the first 10 Fibonacci numbers. 2) You are building a program that needs to extract dates from a block of text. The dates may be in one of the following formats: DD/MM/YYYY
"
"1) Invoice Error Handler Problem: An accounting app processes invoice files. Task: Read a file. Catch and handle missing file or invalid data errors using try-except. 2) You need to extract the domains from a list of email addresses. For example, from user1@example.com and user2@domain.org, you want to extract example.com and domain.org.
"
"1) Numpy Array Tool Problem: A researcher needs to generate test data. Task: Install numpy, generate a random array, and calculate mean. 2) You are building a simple banking system where customers can check their account balance, deposit money, and withdraw money.Write a python code for this scenerio.

"
"1) Temperature Logger Problem: A weather station logs daily temperatures. Task: Write temperatures to a file, then read and display average temperature. 2) You are building a text parser that needs to extract phone numbers from a block of text. Phone numbers can be in various formats such as:123-456-7890, (123) 456-7890
"
"1) Custom Range Iterator Problem: A system needs a custom iterator for number ranges. Task: Implement an iterator that yields numbers in a range with a step. 2) You are building a to-do list application where users can add, view, and remove tasks.Write a python code for this scenerio.
"
"1) Custom Range Iterator Problem: A system needs a custom iterator for number ranges. Task: Implement an iterator that yields numbers in a range with a step. 2) You are tasked with building a system that evaluates students’ grades and determines if they have passed or failed based on their scores.

 "
"1) Payroll Generator Problem: HR needs a way to display salary slips. Task: Create Employee class and override __str__() to print formatted salary details. 2) You are building a library management system where you can add books, search for a book, and issue a book to a user.Write a python code for this scenerio.

"
"1) Word Frequency Analyzer Problem: A file contains a paragraph. Task: Count how many times each word appears. Use a dictionary for frequency. 2) Write a Program to accept a number from 1 to 7 and display the name of the day like 1 for Sunday, 2 for Monday and so on.
"
"1) Customer Order List:
Scenario: You are working on a system that processes customer orders for an online store. You need to store each order's details (customer name, product name, quantity, and price) in a text file, with each piece of information on a separate line. Task: Create a program that writes customer orders to a text file called orders.txt, where each order's information is written on separate lines. Write the following example data: yaml,CopyEdit, John Doe,Laptop,1,1000,Jane Smith,Smartphone,2,600. 2) Sales Data Analysis:Create a sample dataframe with columns: Date, Region, Sales. Use Pandas to calculate total sales per region.

"
"1) Student Grades Report: Scenario: You are tasked with generating a report for students' grades. The report should include the student’s name, course, grade, and the semester. Task: Write a Python program that writes this data to a text file called student_grades.txt. Each student's data should be written on separate lines, and each student’s report should be clearly separated. Example data: css, CopyEdit, Alice Green, Math, A, Fall 2024, Bob, Brown, Science, B+, Fall 2024.
"
"1) Character Range Generator:
Scenario: You need to create an iterator that generates characters between two specified letters, inclusive. The iterator should return the next character in the range each time it is called.
Task: Implement a custom iterator CharRangeIterator that generates characters from a starting character to an ending character in lexicographical order. 2) Bank Account System: Create a class BankAccount that stores account_number and balance as private variables. Implement methods to deposit and withdraw money. Ensure balance can’t be accessed or changed directly.
"
"1) Arithmetic Progression Generator: Scenario: Create an iterator that generates an arithmetic progression. Given a starting number, a common difference, and a maximum limit, the iterator should yield numbers in the arithmetic sequence. Task: Implement a custom iterator ArithmeticProgressionIterator that generates numbers following an arithmetic progression, starting from a given number and increasing by a fixed difference. 2) Animal Sounds Simulation: Define a base class Animal and subclasses Dog, Cat, Cow, each with a speak() method. Use a loop to call speak() on a list of animals and observe polymorphic behavior.

"
"1) Reverse Counting Iterator: Scenario: You are building an iterator that counts down from a specified number to 1. The iterator should yield the next number in the countdown until it reaches 1. Task: Implement a custom iterator ReverseCountIterator that generates numbers in reverse, starting from a specified number and decrementing by 1 with each iteration. 2) Student Marks Sheet: Define a Student class with private marks for subjects. Write setter methods to assign marks and getter methods to calculate average and print results. Prevent negative marks using a check inside the setter.

"
"1) Square Numbers Generator: Scenario: You are tasked with creating an iterator that generates square numbers starting from 1. Each time the iterator is called, it should yield the next square number. Task: Implement a custom iterator SquareIterator that generates the sequence of square numbers (1, 4, 9, 16, etc.) until a maximum number is reached. 2) Employee Management System: Create an Employee class with private attributes: name, salary. Allow updating salary only through a method that checks if the hike is within 20%. Display a message if the hike exceeds the limit.
"
"1) Unit Price Calculation: Scenario: You need to calculate the unit price of an item by dividing the total price by the quantity of items. If the quantity is zero, a ZeroDivisionError will occur. Task: Implement a Python program that calculates the unit price. If the quantity is zero, catch the ZeroDivisionError and print: ""Error: Cannot divide by zero. Quantity must be greater than zero."" 2) ATM PIN Validation: Implement an ATM class with private attribute __pin. Allow PIN validation using a method that accepts user input. Don’t allow direct access or changes to the pin attribute.
"
"1) Speed Calculation: Scenario: You are calculating the speed of a vehicle by dividing the distance by the time taken. If the time taken is zero (i.e., the vehicle is stationary or the time is not recorded), a ZeroDivisionError will be raised. Task: Write a Python program that calculates speed by dividing distance by time. If time is zero, catch the ZeroDivisionError and print: ""Error: Cannot calculate speed. Time cannot be zero."" 2) Library Membership System: A class Member has private data: __membership_id, __books_issued. Use getter methods to retrieve data and setter to increase book count only if it's ≤ 5. 2) Health Tracker App: Create a HealthProfile class with private attributes: __weight, __height.Implement a method to calculate and return BMI. Ensure height is not zero before calculating BMI.


"
"1) Payment Gateway System: Create a common interface make_payment() for different payment types like CreditCard, DebitCard, and UPI. Each class should implement make_payment() differently. Demonstrate how the same method name behaves differently for each payment type. 2) Implement a custom iterator SquareIterator that generates the sequence of square numbers (1, 4, 9, 16, etc.) until a maximum number is reached.
"
"1) Nested Dictionaries    Task: Create a nested dictionary to store employee details (Name, Department, Salary). Write a function to retrieve all employees from a specific department. 2) Animal Sounds Simulation: Define a base class Animal and subclasses Dog, Cat, Cow, each with a speak() method. Use a loop to call speak() on a list of animals and observe polymorphic behavior.

"
"1) Stock Market Data Analysis : Create a sample stock data with fields: Date, Open, Close, Volume Task: Read stock data. Calculate average closing price. 2) E-learning Quiz System: Create a class Question with a method ask(). Subclasses MCQ, TrueFalse, and ShortAnswer override ask() to display the question differently. Show polymorphic behavior while asking various types of questions.

"
"1) Library Management System Task: Create class Book with Title, Author, ISBN, Price (private). Implement getter, setter for Price. Create subclass EBook with File Size attribute. 2) Email Validation Tool Problem: A company needs to validate email addresses from a file. Task: Use regular expressions to check validity of emails from a text file.
"
"1) Student Data Management System Task: a) Create a file students.txt with student details. b) Read student details from students.txt. c) Append new student records. d) Implement exception handling for file errors. 2) Numpy Array Tool Problem: A researcher needs to generate test data. Task: Install numpy, generate a random array, and calculate mean.

"
"1) Package Installation Task: a) Install numpy using PIP. b) Create and display a numpy array with random values. 2) Contact Information Module Problem: Contact details need modular handling. Task: Create a module with functions to add, remove, and search contacts.

"
"1) Vehicle Management System    Task: a) Create a `Vehicle` class with attributes: brand and model. b) Implement polymorphism for a `display()` method to display vehicle details. c) Create a subclass `Car` that adds a fuel type attribute. 2)  Sort and Filter Data    Task: Given a list of dictionaries containing product names and prices, filter out products that cost more than $100 and sort the list by price.

"
"1) 1. Weather Data Analysis::  intitialize a NumPy array temperatures representing daily temperatures over a month (30 values). Task: a) Find the average temperature. b) Identify the hottest and coldest days (index and value). c) Create a new array where temperatures above the monthly average are marked as 1, and below or equal are 0.    2)  Marks Comparison System Problem: A teacher wants to compare marks of two students in three subjects. Task: Take input marks, calculate total, average, and determine who scored higher.
"
"1)  Stock Price Simulator:  Simulate stock prices for 10 days by creating a 1D NumPy array with random floats between $100 and $500. Then: Find the maximum profit possible by buying and selling exactly once. 2) Retail Billing System Problem: A retail store needs a billing system with discount logic. Task: Input item names, prices, and quantities. Compute total bill and apply a discount if the bill exceeds ₹1000.
"
"1) Restaurant Billing System Task: Create a menu as a dictionary with items and prices. Allow users to select multiple items, calculate the total bill, and generate a receipt. 2)  Student Grade Calculator Problem: A university needs to automate grade generation based on exam marks. Task: Get student marks and assign grades (A, B, C, etc.) using conditional statements. Repeat for multiple students.

 "
"1) Shape Class Hierarchy    Task: Create a `Shape` class with an abstract method `area()`. Implement subclasses for different shapes (e.g., `Circle`, `Rectangle`), each with its own area calculation. 2)  File-Based Number Analyzer Problem: A text file contains a list of integers. Task: Create and Read the file and count how many numbers are even and how many are odd.

 "
"1) Create a 2D array of shape (5, 4) with random integers between 0 and 100, representing scores of 5 players across 4 rounds. Then: a) Find the player with the highest total score. b) Identify the round with the lowest average score. 2) Custom Range Iterator Problem: A system needs a custom iterator for number ranges. Task: Implement an iterator that yields numbers in a range with a step.
"
"1) Generate a (5, 3) matrix where each row represents quantities of 3 products bought by 5 customers (random integers between 0 and 10). Generate a (3, 1) matrix for the prices of each product (random floats between $5 and $50). Then: Find the total bill amount for each customer. 2) write a code to check whether two strings are Anagrams. (Anagrams are words or phrases formed by rearranging the letters of another, using all original letters exactly once. For example, ""listen"" and ""silent"" are anagrams.) "